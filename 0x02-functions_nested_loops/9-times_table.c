#include "main.h"

/**
 * times_table - Prints out 9 timestable.
 * Return: nothing.
*/

void times_table(void)
{
	int a, b, i;
	
	i = 0;

	for (a = 0; a < 10; a++)
	{
		for (b = 0; b < 10; b++)
		{
			if (:
			_putchar(i);

			
			if (a <  9)
			{	
				_putchar(',');
				_putchar('  ');
			}

