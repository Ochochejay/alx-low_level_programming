#include "main.h"

/**
 * _strcmp - Compares two string.
 * @s1: first string.
 * @s2: second string.
 * Return: count.
 */

int _strcmp(char *s1, char *s2)
{
	int count;


	/*while (*(s1 + i) != '\0' && *(s2 + i) != '\0')*/
	/*{*/
		count = (*(s1 + 0) - *(s2 + 0));

	/*	i++;*/
	/*}*/

	return (count);
}
